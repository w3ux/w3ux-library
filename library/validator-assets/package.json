{
  "name": "@w3ux/validator-assets-source",
  "license": "GPL-3.0-only",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -fr dist && gulp --silent && cp LICENSE dist",
    "clear": "rm -rf node_modules dist tsconfig.tsbuildinfo"
  }
}
